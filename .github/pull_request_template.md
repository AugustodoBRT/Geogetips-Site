## O que muda

<!-- Uma ou duas frases: o que este PR entrega. -->

Closes #

## Por quê

<!-- O problema resolvido, do ponto de vista de quem usa o site.
     O diff mostra o "o quê"; aqui fica o "por quê". -->

## Como conferir

- [ ] `npx tsc --noEmit`
- [ ] `npm run checar:cores`
- [ ] `NEXT_DIST_DIR=.next-verifica npm run build` (descartando depois
      `tsconfig.json` e `next-env.d.ts`)
- [ ] Telas afetadas conferidas em 1440px e em 375px
- [ ] Se mexeu em cálculo: os números continuam batendo com a planilha

## Telas

<!-- Antes e depois, quando a mudança aparece na tela. Apague a seção se não
     aparece. -->
